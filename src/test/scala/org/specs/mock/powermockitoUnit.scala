package org.specs.mock
import org.specs.runner._
import org.powermock.api.mockito.PowerMockito.mockStatic
import org.powermock.api.mockito.PowerMockito.verifyStatic

class powermockitoUnit extends Specification with Mockito with JUnit {
//   object Service {
//     def say(s: String) = s
//   }
//   class Service 
//   val expected = "Hello world"
//   val argument = "hello"
//
//   def mockStatic[T](implicit m: scala.reflect.Manifest[T]): T = mockStatic(m).asInstanceOf[T]
//   mockStatic[Service]
//   Service.say(argument) returns expected
//   Service.say(argument)
//   Service.say(argument) was called

                // Verification is done in two steps using static methods.
//                verifyStatic(StaticService.class);
  //              StaticService.say(argument);

}
